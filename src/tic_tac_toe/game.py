class Gameplay:
    def __init__(self):
        def winCheck:

        def endgame(board):
            win = TrueBe
            draw = True
            # check if board full
            for i in board:
                for j in board[i]:
                    if board[i][j] != " ":
                        count += 1
            if count = 9:
                #check if win

            for i in board:
                if board[i][0] == board[i][1] == board[i][2]:
                    if board[i][0] != " ":
                        win = True
            # check rows
            # check columns
            # check diagonal