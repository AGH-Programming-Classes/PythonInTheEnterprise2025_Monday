class Gameplay:
    def __init__(self):
        def winCheck:

        def endgame(board):
            win = True
            draw = True
            # check if board full
            for i in board:
                for j in board[i]:
                    if board[i][j] != " ":
                        count += 1
            if count = 9:
                #check if win

            # check rows
            # check columns
            # check diagonal